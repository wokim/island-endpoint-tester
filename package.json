{
  "name": "island-endpoint-tester",
  "version": "2.0.0",
  "description": "use with island",
  "main": "dist/index.js",
  "author": "wokim <wonshikkim.kr@gmail.com>",
  "scripts": {
    "prepublish": "tsc -p $(pwd)",
    "build": "tsc -p $(pwd)",
    "clean": "rm -rf ./dist && rm -rf ./node_modules",
    "test": "tsc -p $(pwd) && jasmine"
  },
  "license": "MIT",
  "dependencies": {
    "deprecated-decorator": "^0.1.6",
    "island": "^3.6.0",
    "schema-inspector": "^1.6.8"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.8",
    "@types/lodash": "^4.14.44",
    "@types/node": "^10.3.1",
    "@types/qs": "^6.5.1",
    "jasmine": "^3.1.0",
    "typescript": "^3.0.1"
  }
}
